name: CI/CD Pipeline

on:
  push:
    branches:
      - "**"

jobs:
  metadata:
    runs-on: ubuntu-latest
    outputs:
      build_datetime: ${{ steps.metadata.outputs.build_datetime }}
      build_timestamp: ${{ steps.metadata.outputs.build_timestamp }}
      build_epoch: ${{ steps.metadata.outputs.build_epoch }}
      version: ${{ steps.metadata.outputs.version }}
    steps:
      - uses: actions/checkout@v3
      - id: metadata
        run: |
          datetime=$(date -u +'%Y-%m-%dT%H:%M:%S%z')
          echo "build_datetime=$datetime" >> $GITHUB_OUTPUT
          echo "build_timestamp=$(date --date=$datetime -u +'%Y%m%d%H%M%S')" >> $GITHUB_OUTPUT
          echo "build_epoch=$(date --date=$datetime -u +'%s')" >> $GITHUB_OUTPUT
          echo "version=$(cat VERSION)" >> $GITHUB_OUTPUT
      - name: List variables
        run: |
          export BUILD_DATETIME="${{ steps.metadata.outputs.build_datetime }}"
          export BUILD_TIMESTAMP="${{ steps.metadata.outputs.build_timestamp }}"
          export BUILD_EPOCH="${{ steps.metadata.outputs.build_epoch }}"
          export VERSION="${{ steps.metadata.outputs.version }}"
          make list-variables
  commit-stage:
    needs: [metadata]
    uses: ./.github/workflows/stage-commit.yaml
    with:
      build_datetime: "${{ needs.metadata.outputs.build_datetime }}"
      build_timestamp: "${{ needs.metadata.outputs.build_timestamp }}"
      build_epoch: "${{ needs.metadata.outputs.build_epoch }}"
      version: "${{ needs.metadata.outputs.version }}"
  test-stage:
    needs: [metadata, commit-stage]
    uses: ./.github/workflows/stage-test.yaml
    with:
      build_datetime: "${{ needs.metadata.outputs.build_datetime }}"
      build_timestamp: "${{ needs.metadata.outputs.build_timestamp }}"
      build_epoch: "${{ needs.metadata.outputs.build_epoch }}"
      version: "${{ needs.metadata.outputs.version }}"
  build-stage:
    needs: [metadata, test-stage]
    uses: ./.github/workflows/stage-build.yaml
    with:
      build_datetime: "${{ needs.metadata.outputs.build_datetime }}"
      build_timestamp: "${{ needs.metadata.outputs.build_timestamp }}"
      build_epoch: "${{ needs.metadata.outputs.build_epoch }}"
      version: "${{ needs.metadata.outputs.version }}"
